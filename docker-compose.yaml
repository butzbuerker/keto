version: "3.3"
services:
  keto:
    build: .
    container_name: keto
    volumes:
      - "${SOURCE_PATH}:/data/source:rw"
      - "${TARGET_PATH}:/data/target:rw"
    restart: unless-stopped
    environment:
      - WEBHOOK_URL=${WEBHOOK_URL}
    env_file:
      - .env
